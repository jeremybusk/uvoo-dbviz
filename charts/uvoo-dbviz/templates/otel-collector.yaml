{{- if and .Values.demo.enabled .Values.demo.otelCollector.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "uvoo-sqviz.otelCollectorName" . }}
  labels:
    {{- include "uvoo-sqviz.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
data:
  config.yaml: |
    receivers:
      otlp:
        protocols:
          http:
            endpoint: 0.0.0.0:4318
          grpc:
            endpoint: 0.0.0.0:4317

    processors:
      batch:
        timeout: 2s

    extensions:
      health_check:
        endpoint: 0.0.0.0:13133

    exporters:
      clickhouse:
        endpoint: tcp://{{ include "uvoo-sqviz.fullname" . }}-clickhouse:9000?dial_timeout=10s
        database: {{ .Values.config.clickhouseDatabase }}
        username: {{ .Values.config.clickhouseUser }}
        password: ${env:CLICKHOUSE_PASSWORD}
        create_schema: true
        logs_table_name: otelcol_logs
        traces_table_name: otelcol_traces
        metrics_tables:
          gauge:
            name: otelcol_metrics_gauge
          sum:
            name: otelcol_metrics_sum
          summary:
            name: otelcol_metrics_summary
          histogram:
            name: otelcol_metrics_histogram
          exponential_histogram:
            name: otelcol_metrics_exp_histogram
        timeout: 5s

    service:
      extensions: [health_check]
      pipelines:
        logs:
          receivers: [otlp]
          processors: [batch]
          exporters: [clickhouse]
        traces:
          receivers: [otlp]
          processors: [batch]
          exporters: [clickhouse]
        metrics:
          receivers: [otlp]
          processors: [batch]
          exporters: [clickhouse]
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "uvoo-sqviz.otelCollectorName" . }}
  labels:
    {{- include "uvoo-sqviz.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
spec:
  type: {{ .Values.demo.otelCollector.service.type }}
  ports:
    - name: otlp-grpc
      port: {{ .Values.demo.otelCollector.service.grpcPort }}
      targetPort: otlp-grpc
    - name: otlp-http
      port: {{ .Values.demo.otelCollector.service.httpPort }}
      targetPort: otlp-http
  selector:
    app.kubernetes.io/name: {{ include "uvoo-sqviz.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: otel-collector
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "uvoo-sqviz.otelCollectorName" . }}
  labels:
    {{- include "uvoo-sqviz.labels" . | nindent 4 }}
    app.kubernetes.io/component: otel-collector
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "uvoo-sqviz.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: otel-collector
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "uvoo-sqviz.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: otel-collector
    spec:
      {{- if .Values.clickhouse.enabled }}
      initContainers:
        - name: wait-for-clickhouse
          image: {{ .Values.clickhouse.image | quote }}
          env:
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "uvoo-sqviz.fullname" .) .Values.secret.existingSecret }}
                  key: SQVIZ_CLICKHOUSE_PASSWORD
          command:
            - /bin/sh
            - -c
            - |
              until clickhouse-client --host {{ include "uvoo-sqviz.fullname" . }}-clickhouse --port 9000 --password "$CLICKHOUSE_PASSWORD" --query "SELECT 1"; do
                sleep 2
              done
      {{- end }}
      containers:
        - name: otel-collector
          image: {{ .Values.demo.otelCollector.image | quote }}
          args: ["--config=/etc/otelcol/config.yaml"]
          env:
            - name: CLICKHOUSE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "uvoo-sqviz.fullname" .) .Values.secret.existingSecret }}
                  key: SQVIZ_CLICKHOUSE_PASSWORD
          ports:
            - name: otlp-grpc
              containerPort: 4317
            - name: otlp-http
              containerPort: 4318
            - name: health
              containerPort: 13133
          startupProbe:
            httpGet:
              path: /
              port: health
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: health
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 30
            periodSeconds: 20
          volumeMounts:
            - name: config
              mountPath: /etc/otelcol
              readOnly: true
      volumes:
        - name: config
          configMap:
            name: {{ include "uvoo-sqviz.otelCollectorName" . }}
{{- end }}
